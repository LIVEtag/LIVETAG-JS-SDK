# Config

Configuration for SDK initialization.

```javascript
initLivetag({
  projectId: '{projectId}',
});

// or

Livetag.init({
  projectId: '{projectId}',
});
```

## projectId

- Type: `string`
- Default: empty
- Required: true

Specify project id.
